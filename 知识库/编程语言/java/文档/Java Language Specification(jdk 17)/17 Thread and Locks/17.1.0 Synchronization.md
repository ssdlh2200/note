# Synchronization
## Monitor
Java中的synchronizeation是通过monitor实现的
- Java中每个对象都有一个monitor
- 一个线程可以锁定或者解锁这个monitor
- 同一时间只有一个线程可以持有该monitor的锁，其它线程获取会被阻塞
- 同一个线程可以多次锁定同一个monitor，每次解锁只需要撤销一次锁定
## Object Header


Java对象在内存的布局示意图
```

```
